<source>
  @type  forward
  @id    input1
  @label @mainstream
  port  24224
</source>

<filter **>
  @type stdout
</filter>

<label @mainstream>
  <match **>
    @type copy
    <store>   
      <buffer time>
        @type file
        path /data
        flush_mode immediate
        timekey 2m
        timekey_wait 30s
      </buffer>
      @type splunkhec
      host splunk
      protocol http
      port 8088
      token bb3714bf-b59c-4971-94b7-30c9244c803a
      sourcetype _json
      usejson true
      send_event_as_json true
    </store>
    <store>
      @type stdout
    </store>
  </match>
</label>